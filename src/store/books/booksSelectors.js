export const booksSelector = state => state.books.items;
export const totalItemsSelector = state => state.books.totalItems;
export const paramsSelector = state => state.books.params;
export const loadingSelector = state => state.books.loading;
